# Development Profile - Disables OAuth2 for testing
# To use this profile, run: mvn spring-boot:run -Dspring-boot.run.profiles=dev

# Disable OAuth2 by not including any OAuth2 properties
# This prevents Spring from initializing OAuth2 client registration

# Logging for debugging
logging.level.com.example.calendar=DEBUG
logging.level.org.springframework.security=DEBUG

# All other settings inherited from application.properties
